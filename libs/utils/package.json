{
  "name": "utils",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/utils.umd.cjs",
  "module": "./dist/utils.js",
  "types": "./dist/main.d.ts",
  "scripts": {
    "dev": "pnpm run build --watch",
    "start": "pnpm run build",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "phoenix": "rm -f pnpm-lock.yaml && rm -rf ./node_modules && pnpm install"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vite-plugin-dts": "^3.9.0"
  }
}
